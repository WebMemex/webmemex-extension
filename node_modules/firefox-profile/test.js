const FP = require('./lib/firefox_profile');

const wait = ms => new Promise(resolve => setTimeout(resolve, ms));

const fp = new FP();

fp.encoded(function(zip) { console.log('here>>>');});
wait(10000).then(function() { process.exit(66);});
